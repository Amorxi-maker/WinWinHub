你是一位精通现代静态单页面应用（SPA）开发的资深前端架构师与顶级 UI 设计师。请基于我现有的单页面应用架构，彻底重构并升级整个网站（包含 index.html、main.css 和 data.js），将其重塑为“现代极简轻奢天蓝杂志社论风（Sky Blue Luxury Editorial Style）”。

必须严格遵循以下全新的交互逻辑与视觉规范进行全面重构：

### 🎨 一、 视觉风格、色彩空间与组件克制规范
1. 色彩空间：整体以纯净利落的【天蓝色（Sky Blue）】为主调。
   - 【核心融合背景色】：为了让打招呼的小人视频与网页完美融为一体，首页 Hero 视觉区域的背景色设为纯白色（#FFFFFF），杜绝任何色差。
   - 文字颜色：使用深邃的高对比暗夜海军蓝墨黑（#061124）。
   - 品牌高亮/点缀色：经典蔚蓝色（#3A8BCC），用于核心标签、按钮悬停、活动标记及核心线条。
2. 排版美学：
   - 大标题：通过 Google Fonts 引入高质感英文衬线体 'Playfair Display'，加宽字母间距。
   - 中文排版：全局配合优雅的宋体/衬线字（如 "Songti SC", "Noto Serif CJK SC", serif）。
   - 正文与数据：使用极简无衬线体（如 'Inter', sans-serif）。
3. 【核心修正：视频小人克制居中】：
   - 首页的动态小人视频（images/豆包.mp4）必须严格适应网页及容器大小，绝对禁止撑满整个屏幕。
   - 视频容器必须限制最大宽度（如 max-width: 320px），并在布局中保持“绝对居中”，表现得像一个精致、克制的动态流媒体徽章，配合 HTML5 属性（autoplay, loop, muted, playsinline）无缝循环。

### 🎬 二、 首页 Hero 区域：错落入场动画
1. 【高级社论风错落入场动效（Editorial Fade Up）】：
   - 编写一段基于 CSS `@keyframes` 的平滑联动入场动画。当页面加载完毕时触发。
   - 居中的动态小人视频率先从下方升起，伴随轻微的模糊淡入；旁边的文字标题与引言设置合理的动画延迟（Animation Delay），形成视觉上的先后滚动错落层次。

### 📊 三、 核心板块重构：Categories 独立主页与 5 列网格
1. 导航路由逻辑修正：
   - 点击顶部导航栏的 “CATEGORIES” 按钮，**必须无刷新平滑流转到独立的 Categories 汇总主页（#categories）**，该页面集中展示 TV Series、Variety Shows、Stage Focus 等大分类卡片，**绝对禁止直接跳回网站首页**。
2. 【核心排版修正：单排五列（5-Column Grid）】：
   - 进入任意具体的子集页面（例如 TV Series 详情页或大分类卡片列表），卡片排版布局必须从原本的 3 列升级为**极为紧凑精细的“单排 5 列”网格布局（grid-template-columns: repeat(5, 1fr)）**。在移动端等小屏幕下则自动弹性平滑降级（如 2 列或 1 列）。

### 📂 四、 核心功能重构：Gallery 深度子图库（直接查看与下载）
1. 路由流转与多级数据结构：
   - 在数据层（data.js）中，Gallery 下的每个分类（如古装写真 guzhuang、私服 sifu）必须拥有一个深度的 `images` 图片路径数组。
   - 用户在 Gallery 主页点击某个大分类卡片时，触发路由流转（如 `#gallery/guzhuang`），无刷新切换至该分类的**专属子集图库页面**。
2. 交互美学：
   - 子图库页面内**直接瀑布流或网格化显示该分类下的所有高清图片，绝对禁止显示任何外部链接或跳转按钮**。
   - 每一张图片卡片上必须优雅集成两个核心轻奢交互：
     - 【点击查看】：点击图片任意区域可唤起一个全屏大图灯箱（Lightbox Overlay），支持高清缩放和一键关闭。
     - 【直接下载】：图片下方提供一个极简的天蓝色胶囊“DOWNLOAD”按钮。点击该按钮时，通过 JavaScript 动态创建 `<a>` 标签并触发 `download` 属性，让粉丝能够一键将高清原图无缝下载保存到本地系统。

### 📂 五、 导航菜单、About 页面与全局搜索
1. ABOUT 独立页面：点击 “ABOUT” 触发 `MapsTo('about')`。页面最中间核心区域独立呈现“官方公告 / 站务更新日志”核心卡片，纵向向下拉动页面平滑过渡到“粉丝留言板 (Guestbook)”。留言板支持客户端 JavaScript 动态追加（append）并支持本地模拟多用户输入。
2. 全局高性能本地模糊搜索算法 (Fuzzy Search)：点击右上角搜索按钮唤起全屏搜索覆盖层（Search Overlay）。用户输入部分关联字符，即可实时模糊检索底层数据对象中的所有影视剧名、角色名、个人档案属性及演艺经历表格数据，点击结果项可无刷新平滑跳转。

### 🎬 六、 Fan-made 板块：YouTube API 动态数据联动
1. 粉丝自制（Fan-made）作品卡片接入 YouTube Data API v3，异步检索关于关键词 `"WINWIN 董思成"` 的最新高热度视频，自动提取高清缩略图进行杂志化排版。
2. 必须编写 `try...catch` 块。当接口因超出每日免费额度或网络问题请求失败时，系统自动平滑降级调用本地备份缓存数组进行数据渲染，确保页面永不空白。

### 🛠️ 七、 技术实现要求
- 必须保持纯前端单页面静态应用（SPA）架构，使用原生 Vanilla JavaScript（ES6+）管理基于 `window.location.hash` 的路由状态，不引入任何复杂的后端框架。
- 整个 `index.html` 的骨架需保持纯净高易读性，将所有可配置的文本与多级图库资产统一剥离并封装在外部 `data.js` 数据对象中。
- 请直接输出满足上述所有全新业务逻辑与网格布局的、可直接跑通的重构方案。